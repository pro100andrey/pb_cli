targets:
  $default:
    builders:
      freezed:
        generate_for:
          - lib/src/state/**.dart
        options:
          runs_before:
            - json_serializable
          map: false
          when:
            when: false
            maybe_when: false
            when_or_null: false
